<div class="container">
  <h2>Add new cocktail:</h2>
  <hr>
  <%= simple_form_for @cocktail do |f| %>
    <%= f.input :name, label: 'Your cocktail should not exist already, Name cannot be blank', error: 'Please read the abovementionned criteria' %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
  <%= link_to 'Return to list', root_path %>
</div>
